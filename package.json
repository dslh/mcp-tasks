{
  "name": "mcp-tasks",
  "version": "1.0.0",
  "description": "A simple MCP server for weekly task tracking",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "bun run dist/index.js",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.15.0"
  },
  "devDependencies": {
    "@types/node": "20.19.4",
    "typescript": "5.8.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
